package org.hdm.tenant.service;

import org.hdm.tenant.objects.Tenant;

public class HDMTenantService implements IHDMTenantService {

    @Override
    public Boolean addTenant(Tenant tenant) {
        return null;
    }
}
